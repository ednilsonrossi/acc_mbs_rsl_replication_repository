# Search String Complete
```
(
  microservice OR microservices
  OR "microservice architecture" OR "microservices architecture"
  OR "microservice-based system" OR "microservice-based architecture"
  OR "cloud-native architecture" OR "cloud-native system" OR "cloud-native application"
  OR "service-oriented architecture" OR SOA
  OR "event-driven" OR "event-driven architecture" OR EDA
  OR "asynchronous communication" OR "asynchronous messaging"
  OR "message-based" OR "message-oriented middleware"
  OR "message broker" OR "message queue" OR "event streaming"
  OR "publish/subscribe" OR "publish-subscribe" OR "pub-sub" OR "pubsub"
)
AND
(
  "architectural checking" OR "architecture checking"
  OR "architectural conformance"  OR "architecture conformance"
  OR "architectural conformance checking" OR "architecture conformance checking"
  OR "architectural compliance" OR "architecture compliance"
  OR "architectural consistency" OR "architecture consistency"
  OR "architectural drift" OR "architecture drift"
  OR "architectural violation" OR "architecture violation"
  OR "architectural erosion" OR "architecture erosion"
  OR "conformance analysis" OR "conformance verification" OR "conformance checking" OR "conformance assessment"
  OR "design rule checking" OR "architecture rule enforcement"
)
```


## Scopus
Used based string on Article title, Abstract and Keywords.
```
( 
  TITLE-ABS-KEY (
    microservice OR microservices 
    OR "microservice architecture" OR "microservices architecture" 
    OR "microservice-based system" OR "microservice-based architecture" 
    OR "cloud-native architecture" OR "cloud-native system" OR "cloud-native application" 
    OR "service-oriented architecture" 
    OR "event-driven" OR "event-driven architecture" 
    OR "asynchronous communication" OR "asynchronous messaging" 
    OR "message-based" OR "message-oriented middleware" 
    OR "message broker" OR "message queue" OR "event streaming" 
    OR "publish/subscribe" OR "publish-subscribe" OR "pub-sub" OR "pubsub" 
  ) 
) 
AND 
( 
  TITLE-ABS-KEY ( 
    "architectural checking" OR "architecture checking" 
    OR "architectural conformance" OR "architecture conformance" 
    OR "architectural conformance checking" OR "architecture conformance checking" 
    OR "architectural compliance" OR "architecture compliance" 
    OR "architectural consistency" OR "architecture consistency" 
    OR "architectural drift" OR "architecture drift" 
    OR "architectural violation" OR "architecture violation" 
    OR "architectural erosion" OR "architecture erosion" 
    OR "conformance analysis" OR "conformance verification" OR "conformance checking" OR "conformance assessment" 
    OR "design rule checking" OR "architecture rule enforcement" 
  ) 
)
```



## ACM Digital Library
```
(
 (Title:(
    microservice*
    OR cloud-native*
    OR "service-oriented architecture"
    OR event-driven OR "event-driven architecture"
    OR "asynchronous communication" OR "asynchronous messaging"
    OR "message-based" OR "message-oriented middleware"
    OR "message broker" OR "message queue" OR "event streaming"
    OR "publish/subscribe" OR "publish-subscribe" OR "pub-sub" OR pubsub
 ))
 OR
 (Abstract:(
    microservice*
    OR cloud-native*
    OR "service-oriented architecture"
    OR event-driven OR "event-driven architecture"
    OR "asynchronous communication" OR "asynchronous messaging"
    OR "message-based" OR "message-oriented middleware"
    OR "message broker" OR "message queue" OR "event streaming"
    OR "publish/subscribe" OR "publish-subscribe" OR "pub-sub" OR pubsub
 ))
 OR
 (Keyword:(
    microservice*
    OR cloud-native*
    OR "service-oriented architecture"
    OR event-driven OR "event-driven architecture"
    OR "asynchronous communication" OR "asynchronous messaging"
    OR "message-based" OR "message-oriented middleware"
    OR "message broker" OR "message queue" OR "event streaming"
    OR "publish/subscribe" OR "publish-subscribe" OR "pub-sub" OR pubsub
 ))
)
AND
(
 (Title:(
    "architectural checking" OR "architecture checking"
    OR "architectural conformance" OR "architecture conformance"
    OR "architectural conformance checking" OR "architecture conformance checking"
    OR "architectural compliance" OR "architecture compliance"
    OR "architectural consistency" OR "architecture consistency"
    OR "architectural drift" OR "architecture drift"
    OR "architectural violation" OR "architecture violation"
    OR "architectural erosion" OR "architecture erosion"
    OR "conformance analysis" OR "conformance verification"
    OR "conformance checking" OR "conformance assessment"
    OR "design rule checking" OR "architecture rule enforcement"
 ))
 OR
 (Abstract:(
    "architectural checking" OR "architecture checking"
    OR "architectural conformance" OR "architecture conformance"
    OR "architectural conformance checking" OR "architecture conformance checking"
    OR "architectural compliance" OR "architecture compliance"
    OR "architectural consistency" OR "architecture consistency"
    OR "architectural drift" OR "architecture drift"
    OR "architectural violation" OR "architecture violation"
    OR "architectural erosion" OR "architecture erosion"
    OR "conformance analysis" OR "conformance verification"
    OR "conformance checking" OR "conformance assessment"
    OR "design rule checking" OR "architecture rule enforcement"
 ))
 OR
 (Keyword:(
    "architectural checking" OR "architecture checking"
    OR "architectural conformance" OR "architecture conformance"
    OR "architectural conformance checking" OR "architecture conformance checking"
    OR "architectural compliance" OR "architecture compliance"
    OR "architectural consistency" OR "architecture consistency"
    OR "architectural drift" OR "architecture drift"
    OR "architectural violation" OR "architecture violation"
    OR "architectural erosion" OR "architecture erosion"
    OR "conformance analysis" OR "conformance verification"
    OR "conformance checking" OR "conformance assessment"
    OR "design rule checking" OR "architecture rule enforcement"
 ))
)
```



## IEEE Xplore
```
(
  microservice OR microservices
  OR "microservice architecture" OR "microservices architecture"
  OR "microservice-based system" OR "microservice-based architecture"
  OR "cloud-native architecture" OR "cloud-native system" OR "cloud-native application"
  OR "service-oriented architecture"
  OR "event-driven" OR "event-driven architecture"
  OR "asynchronous communication" OR "asynchronous messaging"
  OR "message-based" OR "message-oriented middleware"
  OR "message broker" OR "message queue" OR "event streaming"
  OR "publish/subscribe" OR "publish-subscribe" OR "pub-sub" OR "pubsub"
)
AND
(
  "architectural checking"  OR "architecture checking"
  OR "architectural conformance"  OR "architecture conformance"
  OR "architectural conformance checking" OR "architecture conformance checking"
  OR "architectural compliance" OR "architecture compliance"
  OR "architectural consistency" OR "architecture consistency"
  OR "architectural drift" OR "architecture drift"
  OR "architectural violation" OR "architecture violation"
  OR "architectural erosion" OR "architecture erosion"
  OR "conformance analysis" OR "conformance verification" OR "conformance checking" OR "conformance assessment"
  OR "design rule checking" OR "architecture rule enforcement"
)
```



## Web of Science
```
(
  microservice OR microservices
  OR "microservice architecture" OR "microservices architecture"
  OR "microservice-based system" OR "microservice-based architecture"
  OR "cloud-native architecture" OR "cloud-native system" OR "cloud-native application"
  OR "service-oriented architecture" OR SOA
  OR "event-driven" OR "event-driven architecture" OR EDA
  OR "asynchronous communication" OR "asynchronous messaging"
  OR "message-based" OR "message-oriented middleware"
  OR "message broker" OR "message queue" OR "event streaming"
  OR "publish/subscribe" OR "publish-subscribe" OR "pub-sub" OR "pubsub"
)
AND
(
  "architectural checking" OR "architecture checking"
  OR "architectural conformance"  OR "architecture conformance"
  OR "architectural conformance checking" OR "architecture conformance checking"
  OR "architectural compliance" OR "architecture compliance"
  OR "architectural consistency" OR "architecture consistency"
  OR "architectural drift" OR "architecture drift"
  OR "architectural violation" OR "architecture violation"
  OR "architectural erosion" OR "architecture erosion"
  OR "conformance analysis" OR "conformance verification" OR "conformance checking" OR "conformance assessment"
  OR "design rule checking" OR "architecture rule enforcement"
)
```